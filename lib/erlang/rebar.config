{erl_opts, [debug_info]}.

{profiles, [
    {test, [
        {deps, [meck]},
        {eunit_tests, [
            {dir, "test"},
            {dir, "test/gen-erlang"}
        ]}
    ]}
]}.

{project_plugins, [
    {erlfmt, "1.3.0"}
]}.
